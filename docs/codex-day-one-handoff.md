# Codex Continuation: Santa Day One

Continue the existing Miami Papa Noel project in C:\XIV\santa. This is
implementation work toward the first genuine operator-assisted customer AI
workflow, not a request for another architecture proposal or a new project.

## Read and Coordinate

Read C:\XIV\START_HERE.md, CLAUDE.md, loop.md,
docs/santa-agent-workboard.md, docs/tonight-launch-checklist.md,
docs/production-launch.md and tools/web_inquiry/README.md. Verify the Git
root and origin, inspect status/recent commits, and claim a bounded task on
the shared workboard before editing. Never use the inherited rally-coach
directory or recreate the old Santa folder. Preserve other workers' edits.

## Verified Starting Point

- Local checkpoint cb03e99 contains the inquiry/review queue, maintenance
  tools, uninstalled hosting templates, route/quote fixes and North Pole
  roles. It was not pushed or deployed by this coordinator.
- Claude's launch-preflight handoff is already reviewed and integrated.
  R1/R2 fixes and suite registration are done. Do not repeat that task.
- Checkpoint result: 564 passed, 5 skipped, 41 subtests; all 20 suites and
  ops checks passed. This is a dated result, not proof for future edits.
- New local, uncommitted --check-model changes: 71 triage tests passed;
  full ops check 583 passed, 5 skipped, 41 subtests, all 20 suites and ops
  steps PASS. Missing configuration smoke exits 1 without a request/log.
  Claude also delivered docs/day-one-operator-card.md and reported fresh
  synthetic offline desktop/mobile browser QA in the workboard. Review
  those results before deciding any repeat verification is needed.
- The evidence clock now shares a predicate between triage --status and
  the submission validator: valid real model-backed gated reviewed/sent
  records only, with a 15-full-day target from the send timestamp. An old
  fallback or unsent row cannot age a fresh model reply. Neither checker
  certifies continuous operation or OPN acceptance from elapsed time.
- Latest verification after the clock fix: 626 passed, 5 skipped, 52
  subtests; all 20 suites and ops checks PASS. Files remain uncommitted.
  The goal is blocked on private replacement-key setup / a real model
  check and genuine business use, not on another scaffold. Do not repeat
  completed checks or generate production records while those inputs are
  unchanged. Keep the full goal intact when the owner resumes it.
- The September 5 gpt-4.1-mini-2025-04-14 Santa test returned HTTP 429,
  credit_balance_exhausted, then safely fell back to offline EN/ES drafts.
- A separately authorized exact curl request to /v1/responses using
  gpt-5.6-luna, input "write a haiku about ai", store true, returned HTTP
  200 and a completed haiku: 12 input tokens, 99 output tokens, 111 total.
- That proves this exact synthetic request succeeded. It does not prove
  free ongoing access, Santa's structured/gated workflow, or production use.
  Do not assume the earlier 429 means every model is unavailable.
- Santa's runtime/model configuration has not been changed. No production
  inquiry is recorded. The new inquiry backend is not publicly deployed.
- The workboard and launch checklist contain newer uncommitted diagnostic
  notes. Preserve them. Claude outputs/ and _to_delete/ are unrelated.

## Next Work, in Order

1. Prepare a private runtime configuration using the exact selected model
   gpt-5.6-luna. A test credential was exposed in chat; do not recover it
   from history, put it in files, or reuse it in scheduled jobs. The owner
   should rotate it and enter a replacement privately. Check presence only;
   never print credentials, prefixes, hashes or raw provider errors.
2. Confirm authorization and available usage before another API request.
   The existing CLI now has `python -B tools/triage/triage.py --check-model`.
   Run it from the privately configured terminal: one fixed synthetic
   Spanish home-visit inquiry through triage.build_record, strict schema
   and all six gates. It writes no local inquiry log and exits nonzero on
   fallback. Do not recreate this diagnostic. Its mocked tests do not
   establish actual Luna compatibility. Keep store false as implemented;
   the haiku's store true is not its new policy. No automatic retries.
3. If the actual request exposes a defect, fix the smallest responsible
   adapter/prompt/schema issue and add focused regression tests. Inspect
   synthetic failure details safely. Do not weaken pricing, parity,
   missing-information, confirmation, insurance or payment safeguards to
   obtain a green result. Retain the deterministic fallback.
4. Verify the local operator journey with synthetic state outside Git:
   intake, EN/ES drafts, gate results, explicit review/approval and separate
   manual-send attestation. Test reject/fallback and stale-review behavior.
   Use the existing UI and tooling. Do fresh desktop/mobile visual checks
   when a local preview can be started; record anything not verified.
5. Make the shortest operator launch checklist using existing email/text
   inboxes. A genuine business inquiry must be supplied privately by the
   owner. The human reviews and actually sends the reply; never fabricate
   that action. Record actual model, timestamps and outcome only afterward.
6. After source changes, run focused tests and python scripts/ops_check.py.
   Keep the workboard current with exact results, remaining inputs and the
   next action. Do not repeatedly run the full suite on unchanged blockers.

## Day One and Boundaries

Target genuine operation today if its prerequisites are actually met, not
a deadline-driven status label. The haiku, synthetic demos, health checks,
enabled flags and idle hosting are not customer-use evidence. Do not set
--real, backdate a launch or send a message on the owner's behalf. Explain
that OPN's review determines acceptance. The customer workflow needs to
continue operating across the required period; do not manufacture daily
inquiries on quiet days.

Mrs. Claus owns bilingual communications and a future warm female voice.
Santa Claus owns content; numbered elves handle bookings, road logistics,
outreach and monitoring. North Pole branding never changes real prices,
travel requirements or human approval. BOOKED requires human verification
of the 50% deposit. Only approved Zelle and real Stripe Payment Links apply.

Public 24/7 hosting, Stripe activation, automated calls/texts, automatic
availability/confirmations and social publishing remain separate unfinished
integrations. Do not claim them live or make all of them prerequisites for
the smaller operator-assisted workflow. No new spending, account changes,
pushes, deployment, DNS edits, recording or external sends without specific
authorization. Get a coordinated checkpoint before Git commits. No real
customer data, payment evidence, API keys or private logs may enter Git.

Finish with three plain statements: what now works, what still blocks
genuine use, and the single next owner action. Do not declare the whole
Santa project or the 15-day requirement complete after a synthetic success.
